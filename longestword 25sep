#include <stdio.h>
#include <string.h>

int main()
{
   char str[100],word[100],max[100]=" ";
   int i=0,j=0,words=0;
   printf("enter a sentence:\n");
   fgets(str,sizeof(str),stdin);
   while(str[i]!='\0'){
       if(str[i]!=' '){
           word[j]=str[i];
           j++;
       }
       else{
           word[j]='\0';
           len=strlen(word);
           if(len>max){
               max=len;
               strcpy(longest,word);
           }
           j=0;
       }
       i++
   }
   word[j]='\0';
}
